(define (domain no_name)
	(:requirements :strips :typing :negative-preconditions :mep :no-duplicates)
	(:predicates (owner  ?ag_hhccm - agent ?color_ryaer - color) (free_table) (table  ?color_hjvlf - color))
	(:action look_up
		:act_type ontic
		:parameters (?a1 - agent ?c1 - color)
		:precondition ([?a1](free_table ))
		:effect (when (owner ?a1 ?c1) (owner ?a1 ?c1))
		:observers (and (?a1))

	)
)
