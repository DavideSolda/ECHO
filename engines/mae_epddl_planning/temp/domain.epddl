(define (domain no_name)
	(:requirements :strips :typing :negative-preconditions :mep :no-duplicates)
	(:predicates (p  ?integer_lpbhe - integer) (connected  ?ag_ryxbw - agent ?ag_zukai - agent))
	(:action share_secret
		:act_type announcement
		:parameters (?a1 - agent ?a2 - agent ?v - integer)
		:precondition (and ([?a1](p ?v)) (connected ?a1 ?a2))
		:effect (p ?v)
		:observers (and (?a2))

	)
	(:action peek
		:act_type sensing
		:effect (p uno)
		:observers (and (giangiovanni))

	)
)
