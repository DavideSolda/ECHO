(define (problem no_name)
	(:objects
		red orange - color
	)
	(:domain no_name)
	(:agents alice bob)
	(:depth 2)
	(:dynck false)
	(:init (owner bob red) (owner bob orange) (free_table ) ([bob alice](free_table )) ([bob alice](not (table red))) ([bob alice](not (table orange))))
	(:goal ([alice bob](owner alice red)) ([alice bob](owner alice orange)))
)