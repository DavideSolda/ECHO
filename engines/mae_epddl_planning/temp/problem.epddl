(define (problem no_name)
	(:objects
		red orange yellow black - color
	)
	(:domain no_name)
	(:agents alice bob)
	(:depth 2)
	(:dynck false)
	(:init (owner bob red) (owner bob black) (owner bob orange) (free_table ))
	(:goal ([bob](owner bob red)))
)