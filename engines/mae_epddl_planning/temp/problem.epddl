(define (problem no_name)
	(:objects
		red - color
	)
	(:domain no_name)
	(:agents alice bob)
	(:depth 2)
	(:dynck false)
	(:init (owner bob red) (free_table ) ([bob alice](free_table )))
	(:goal ([alice](owner alice red)))
)